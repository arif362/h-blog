<div>
  <%
    start_time = Time.now.in_time_zone('Asia/Dhaka')
    end_time = 10.minutes.from_now.in_time_zone('Asia/Dhaka')
  %>
</div>
<div> Start Time: <%= start_time.strftime('%I:%M  %p') %> </div>
<div> End Time: <%= (end_time).strftime('%I:%M  %p') %> </div>
<div> Timer is count down : <%= (end_time.to_i - Time.now.in_time_zone('Asia/Dhaka').to_i) / 60 %> </div>
<div class="countdown"></div>



<script>
    let timer2 = "5:01";
    const interval = setInterval(function () {
        const timer = timer2.split(':');
        //by parsing integer, I avoid all extra string processing
        let minutes = parseInt(timer[0], 10);
        let seconds = parseInt(timer[1], 10);
        --seconds;
        minutes = (seconds < 0) ? --minutes : minutes;
        if (minutes < 0) clearInterval(interval);
        seconds = (seconds < 0) ? 59 : seconds;
        seconds = (seconds < 10) ? '0' + seconds : seconds;
        //minutes = (minutes < 10) ?  minutes : minutes;
        $('.countdown').html(minutes + ':' + seconds);
        timer2 = (minutes + ':' + seconds);
    }, 1000);
</script>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js">

</script>
